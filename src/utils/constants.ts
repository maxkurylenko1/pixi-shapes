/** Generation area dimensions */
export const AREA_WIDTH = 800;
export const AREA_HEIGHT = 600;

/** Default config values */
export const DEFAULT_SPAWN_RATE = 1; // shapes per second
export const DEFAULT_GRAVITY = 2; // px per second² acceleration

/** Shape size range */
export const SHAPE_MIN_RADIUS = 20;
export const SHAPE_MAX_RADIUS = 50;
